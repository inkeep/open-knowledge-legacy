diff --git a/Users/edwingomezcuellar/projects/open-knowledge/.claude/worktrees/markdown-source-text-fidelity/node_modules/@tiptap/markdown/.bun-tag-74a5f540ae3a9475 b/.bun-tag-74a5f540ae3a9475
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/Users/edwingomezcuellar/projects/open-knowledge/.claude/worktrees/markdown-source-text-fidelity/node_modules/@tiptap/markdown/.bun-tag-b764bd45a71a3632 b/.bun-tag-b764bd45a71a3632
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/Users/edwingomezcuellar/projects/open-knowledge/.claude/worktrees/markdown-source-text-fidelity/node_modules/@tiptap/markdown/.bun-tag-f8e9e59d9d731477 b/.bun-tag-f8e9e59d9d731477
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.cjs b/dist/index.cjs
index c4c0458169641c021d4f8cffa134be987ab656da..bd4b8c4e464674294ff9c9a9100f6ccb911894f5 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -560,6 +560,11 @@ var MarkdownManager = class {
           type: "text",
           text: _core.decodeHtmlEntities.call(void 0, token.text || "")
         });
+      } else if (token.type === "escape") {
+        result.push({
+          type: "text",
+          text: token.raw || ("\\" + (token.text || ""))
+        });
       } else if (token.type === "html") {
         const raw = ((_b = (_a = token.raw) != null ? _a : token.text) != null ? _b : "").toString();
         const isClosing = /^<\/[\s]*[\w-]+/i.test(raw);
@@ -749,7 +754,7 @@ var MarkdownManager = class {
    */
   encodeTextForMarkdown(text, node, parentNode) {
     const isInsideCode = (parentNode == null ? void 0 : parentNode.type) != null && this.codeTypes.has(parentNode.type) || (node.marks || []).some((m) => this.codeTypes.has(typeof m === "string" ? m : m.type));
-    return isInsideCode ? text : _core.encodeHtmlEntities.call(void 0, text);
+    return text;
   }
   renderNodeToMarkdown(node, parentNode, index = 0, level = 0, meta = {}) {
     var _a;
diff --git a/dist/index.js b/dist/index.js
index fa88ebedab4df982f5395df4b00ec6ee90397257..69ff6e2ffb28d6533bc88f019fb703953d84f6ff 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -560,6 +560,11 @@ var MarkdownManager = class {
           type: "text",
           text: decodeHtmlEntities(token.text || "")
         });
+      } else if (token.type === "escape") {
+        result.push({
+          type: "text",
+          text: token.raw || ("\\" + (token.text || ""))
+        });
       } else if (token.type === "html") {
         const raw = ((_b = (_a = token.raw) != null ? _a : token.text) != null ? _b : "").toString();
         const isClosing = /^<\/[\s]*[\w-]+/i.test(raw);
@@ -749,7 +754,7 @@ var MarkdownManager = class {
    */
   encodeTextForMarkdown(text, node, parentNode) {
     const isInsideCode = (parentNode == null ? void 0 : parentNode.type) != null && this.codeTypes.has(parentNode.type) || (node.marks || []).some((m) => this.codeTypes.has(typeof m === "string" ? m : m.type));
-    return isInsideCode ? text : encodeHtmlEntities(text);
+    return text;
   }
   renderNodeToMarkdown(node, parentNode, index = 0, level = 0, meta = {}) {
     var _a;
diff --git a/src/MarkdownManager.ts b/src/MarkdownManager.ts
index 4e9d7b713571b6e03ec70b2cbbbcaf440f55211a..1b0c4eb737ffa97a45114fcb4719fc1fd198884a 100644
--- a/src/MarkdownManager.ts
+++ b/src/MarkdownManager.ts
@@ -658,6 +658,15 @@ export class MarkdownManager {
           type: 'text',
           text: decodeHtmlEntities(token.text || ''),
         })
+      } else if (token.type === 'escape') {
+        // Patched (source-text fidelity): handle backslash-escape tokens.
+        // marked produces escape tokens for \* \_ \[ etc. We preserve the
+        // full raw form (backslash + char) in the text node so that
+        // serialize outputs it unchanged and the round-trip is byte-exact.
+        result.push({
+          type: 'text',
+          text: token.raw || ('\\' + (token.text || '')),
+        })
       } else if (token.type === 'html') {
         // Handle possible split inline HTML by attempting to detect an
         // opening tag and searching forward for a matching closing tag.
@@ -899,15 +908,15 @@ export class MarkdownManager {
   }
 
   /**
-   * Encode HTML entities in text unless the node is inside a code context
-   * (code mark or code-block parent) where literal characters should be preserved.
+   * Encode text for markdown output.
+   *
+   * **Patched (source-text fidelity):**
+   * Returns text unchanged — entity encoding (&amp; &lt; &gt;) is removed.
+   * Backslash escapes are preserved by storing the full raw form (\* \_ etc.)
+   * in text nodes during parsing, so they pass through serialization as-is.
    */
   private encodeTextForMarkdown(text: string, node: JSONContent, parentNode?: JSONContent): string {
-    const isInsideCode =
-      (parentNode?.type != null && this.codeTypes.has(parentNode.type)) ||
-      (node.marks || []).some(m => this.codeTypes.has(typeof m === 'string' ? m : m.type))
-
-    return isInsideCode ? text : encodeHtmlEntities(text)
+    return text
   }
 
   renderNodeToMarkdown(
