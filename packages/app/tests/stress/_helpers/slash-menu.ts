/**
 * Slash-command menu helpers.
 *
 * Stub scaffolding — US-007 (slash-command.e2e.ts migration) lands the real
 * implementations of `waitForSlashMenuOpen` / `waitForSlashMenuFiltered` /
 * `waitForSlashMenuClosed` per D-Q3 LOCKED (signals based on `[role="listbox"]`
 * visibility + filtered item count).
 *
 * Kept empty intentionally — adding placeholder no-op exports here would
 * mislead consumers. US-007 lands the helpers when the migration begins.
 */

export {};
