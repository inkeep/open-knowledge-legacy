/**
 * Clipboard interaction helpers.
 *
 * Stub scaffolding — US-008 (paste-fidelity.e2e.ts migration) lands the
 * `selectAllAndWaitForSelection` helper that replaces the in-file
 * `Meta+a` → `waitForTimeout(50)` selection-flush pattern.
 */

export {};
