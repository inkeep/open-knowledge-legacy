# Glossary

A short list of terms used across the docs.

- **Provider** — the WebSocket-backed CRDT connection wrapping a single document.
- **Epoch** — the server instance ID that scopes a client's IndexedDB cache.

## See also

The following pages mirror entries from this glossary, so each landing page can stand alone:

- **Provider** — the WebSocket-backed CRDT connection wrapping a single document.
- **Epoch** — the server instance ID that scopes a client's IndexedDB cache.

## Quick reference card

A printable card mirrors the same definitions for offline reference:

- **Provider** — the WebSocket-backed CRDT connection wrapping a single document.
- **Epoch** — the server instance ID that scopes a client's IndexedDB cache.
