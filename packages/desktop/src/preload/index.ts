/**
 * Preload bridge — exposes `window.okDesktop` to the renderer (US-005).
 *
 * Scaffold only for US-003. Full OkDesktopBridge shape + contextBridge wiring
 * lands in US-005 once the shared IPC channel map (US-004) is in place.
 */
export {};
