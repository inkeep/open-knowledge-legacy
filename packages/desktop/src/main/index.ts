/**
 * Main-process entry for `@inkeep/open-knowledge-desktop`.
 *
 * Populated in US-007+ (window manager, utility lifecycle, navigator window,
 * macOS parent-death polling on the child side). Scaffold only for US-003.
 */
export {};
