/**
 * utilityProcess entry — hosts Hocuspocus via `bootServer()` per project window.
 *
 * Scaffold only for US-003. Full IPC handshake (`init` / `ready` / `shutdown`)
 * + parent-death polling + shutdown drain land in US-006 + US-008.
 */
export {};
