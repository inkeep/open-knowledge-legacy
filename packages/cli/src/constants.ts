/** Root directory name for the wiki inside a project. */
export const WIKI_DIR = '.open-knowledge';

/** Auto-generated catalog filename placed in every wiki directory. */
export const CATALOG_FILENAME = 'INDEX.md';

/** Wiki conventions file for agent navigation without MCP. */
export const AGENTS_FILENAME = 'AGENTS.md';

/** Workspace-level config file inside the wiki directory. */
export const CONFIG_FILENAME = 'config.yml';

/** Gitignored directory for derived/cached data. */
export const CACHE_DIR = 'cache';
