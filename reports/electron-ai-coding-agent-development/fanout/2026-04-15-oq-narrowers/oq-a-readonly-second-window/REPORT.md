# OQ-A Narrower: Read-only-second-window reference-app survey

## Key finding
No surveyed app (Obsidian, Logseq, VS Code, Cursor, AFFiNE, Notion Desktop) ships an explicit read-only-second-window mode. Dominant pattern is focus-existing (single-instance). A2 does not exist in the wild. Recommendation: Ship A1 (hard-refuse + focus existing window).

Full evidence from subagent — see meta/runs conversation for the detailed per-app survey.
