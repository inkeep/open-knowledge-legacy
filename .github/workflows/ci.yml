name: CI

# Tier 1 per SPEC §6 R9 (US-016). Runs the canonical `bun run check`
# composition (lint + typecheck + unit + integration + conversion + fidelity)
# plus opt-in coverage (fuzz + stress + Playwright) since those match what
# CLAUDE.md documents as the PR-time expectation. Budget: 15 min (p95 warm
# baseline ≈ 2m30s on local M3 Max; CI runners are ~2× slower; 1.5× headroom).
# Tier 2 (perf regression gate, parse-health gate, parseWithFallback 5× bound)
# runs in `nightly.yml`. Tier 3 (elevated-sample PBT + fuzz + perf trend)
# runs in `weekly.yml`.

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: oven-sh/setup-bun@4bc047ad259df6fc24a6c9b0f9a0cb08cf17fbe5 # v2.0.1
        with:
          bun-version: "1.3.11"
          cache: true
      - run: bun install --frozen-lockfile
      - run: bun run lint

  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        task:
          - typecheck
          - test
          - test:integration
          - test:conversion
          - test:fidelity
          - test:stress:server-authoritative
          - test:fuzz:bridge
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      # Node 22+ is required because turbo's `^build` pulls in
      # `open-knowledge-app#build` (vite build) transitively via the cli
      # package's workspace dep on the app. Vite's config loader evaluates
      # the bundled config with native `import()`, and externalized workspace
      # deps that resolve to `.ts` sources (e.g., `@inkeep/open-knowledge-server`
      # which exports `./src/index.ts`) hit Node's ESM loader. Node 20 throws
      # ERR_UNKNOWN_FILE_EXTENSION; Node 22.6+ strips TypeScript types natively.
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "22"
      - uses: oven-sh/setup-bun@4bc047ad259df6fc24a6c9b0f9a0cb08cf17fbe5 # v2.0.1
        with:
          bun-version: "1.3.11"
          cache: true
      - uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ matrix.task }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-${{ matrix.task }}-
      - run: bun install --frozen-lockfile
      - run: bunx turbo run ${{ matrix.task }}

  playwright:
    runs-on: ubuntu-latest
    # Three browser projects (chromium + webkit + firefox) × 5 E2E files.
    # Empirical runtime on ubuntu-latest:
    #   - playwright install (binaries, cached): ~10s
    #   - playwright install-deps chromium: ~10s
    #   - playwright install-deps webkit: ~16s
    #   - test:e2e: ~24 min (5 `&&`-chained invocations × 3 browsers,
    #     each invocation spins up a fresh webServer due to
    #     `reuseExistingServer: false` for state isolation)
    # Budget 40 min: 25 min observed + ~15 min headroom for slower
    # runners / coldcache / the occasional stress flake re-run. Single-
    # browser baseline was ~6 min (pre-Act-5). test:e2e script
    # consolidation into a single invocation would cut ~5 min by
    # eliminating per-file webServer startup, but is a separate cleanup.
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      # Node 22+ is required here because Vite's config loader writes the
      # bundled config to a .mjs file and evaluates it with native `import()`.
      # Any externalized workspace dep that resolves to a `.ts` source (e.g.,
      # `@inkeep/open-knowledge-server` which exports `./src/index.ts`) gets
      # hit by Node's ESM loader at that import. Node 20 throws
      #   ERR_UNKNOWN_FILE_EXTENSION
      # while Node 22.6+ strips TypeScript types natively and returns the
      # module cleanly. Ubuntu runners default to Node 20, so we upgrade
      # explicitly for the playwright job only.
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: "22"
      - uses: oven-sh/setup-bun@4bc047ad259df6fc24a6c9b0f9a0cb08cf17fbe5 # v2.0.1
        with:
          bun-version: "1.3.11"
          cache: true
      - uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5.0.4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/bun.lock') }}
          # Partial cache hit on minor version bumps is preferable to a
          # full ~200MB re-download of Chromium every time bun.lock changes.
          restore-keys: |
            ${{ runner.os }}-playwright-
      - run: bun install --frozen-lockfile
      # Install all three browsers for cross-browser parity (SPEC §13, QA-046).
      # The playwright.config.ts now defines chromium/webkit/firefox projects;
      # tests fan out across all three automatically.
      #
      # Install strategy (three commands, ordered for speed):
      #   1. `playwright install chromium webkit firefox` — downloads the
      #      browser binaries only; served from the actions/cache above on hit.
      #   2. `playwright install-deps chromium` — installs chromium's system
      #      libs via apt (baseline graphics/audio/X11 stack).
      #   3. `playwright install-deps webkit` — installs webkit's specific
      #      libs (GTK4 + GStreamer + audio codecs + libwoff2dec +
      #      libhyphen + libmanette + libsecret + libflite + libavif). Without
      #      this step, `browserType.launch` fails with "Host system is
      #      missing dependencies to run browsers" listing ~20 libs.
      #   Firefox doesn't need a dedicated install-deps pass on ubuntu-latest
      #   — its required libs overlap with chromium's baseline.
      #
      # Historical footnote: `playwright install --with-deps chromium webkit
      # firefox` (single command) ran a full apt resolution of ALL browsers'
      # system packages (gstreamer, libsdl2, libpipewire, timgm6mb-soundfont,
      # etc.) which took ~25 min on ubuntu-latest mirrors and killed the
      # job before tests started (commits fddd7ed1 → cancelled at 25-min
      # timeout). The split approach is cache-friendly and avoids the apt
      # pathological case.
      - run: bunx playwright install chromium webkit firefox
      - run: bunx playwright install-deps chromium
      - run: bunx playwright install-deps webkit
      - env:
          VITE_PORT: "13579"
        run: bunx turbo run test:e2e
