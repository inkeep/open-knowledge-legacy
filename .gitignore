node_modules/
dist/

# Per-package lockfiles — only root bun.lock should exist in monorepo
packages/*/bun.lock

# Electron desktop build output — electron-vite output + electron-builder dist
packages/desktop/out/
packages/desktop/dist-desktop/

# Turbo cache
.turbo/

# init_spike: accidental tsc JS emit beside src/ (sources are .ts/.tsx; .d.ts may live under src/types/)
init_spike/src/**/*.js
init_spike/src/**/*.js.map
tmp/
.DS_Store
*.log
.next/
.source/
.idea/
.bun/

# TypeScript incremental build cache
*.tsbuildinfo

# Local review artifacts — generated on demand, not meant to be committed
.claude/pr-diff/
.claude/skills/pr-context/
.claude/worktrees/
.worktrees/

# Runtime content file — accumulates test artifacts from dev/agent-sim runs
init_spike/content/test-doc.md

# Wiki/content runtime directory (pre-wiki→content rename on main)
packages/content/

# Playwright generated artifacts (test runs + stored traces + last-run manifest)
# These directories are created by `node_modules/@playwright/test/cli.js test` and
# must be excluded from biome/lint and git alike.
test-results/
packages/app/test-results/
playwright-report/
blob-report/

# Open Knowledge shadow repo — bare git repo at `.openknowledge/` in standalone
# mode (no project `.git/`) or at `.git/openknowledge/` in integrated mode.
# Standalone mode MUST be gitignored at the project root: the shadow repo
# contains binary pack objects, per-writer WIP refs naming pid/hostname/
# worktreeRoot, and a lock file with the developer's absolute path. Integrated
# mode is inside `.git/` which git already ignores. See `packages/server/src/
# shadow-repo.ts` and CLAUDE.md "Shadow repo & branch runtime".
.openknowledge/

# Editor workspace files
.vscode/
.mcp.json
.cursor/mcp.json
.claude/launch.json

# Benchmark harness output — per-run artifacts, published as CI artifacts.
# Baselines live in specs/.../evidence/perf-baseline-measured.md instead.
packages/core/tests/perf/results.*.json

/test-doc.md

# Open Knowledge per-machine runtime state — tokens never live here, but
# sync state, server locks, and derived caches are local-only.
.open-knowledge/sync-state.json
.open-knowledge/server.lock
.open-knowledge/cache/
